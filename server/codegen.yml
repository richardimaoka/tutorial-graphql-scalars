overwrite: true
schema: "schema.gql"
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      avoidOptionals: true
      scalars:
        EmailAddress: string
hooks:
  afterOneFileWrite:
    - npx prettier --write
